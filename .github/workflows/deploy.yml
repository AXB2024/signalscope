name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: []
    steps:
      - uses: actions/checkout@v4

      - name: Build and push Docker images
        run: |
          echo "Add your Docker Hub / ECR push steps here"

      - name: Deploy to server
        run: |
          echo "Add your SSH deploy / ECS update steps here"
